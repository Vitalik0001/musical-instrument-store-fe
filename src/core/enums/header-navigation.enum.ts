export enum HeaderNavigationButtonId {
    Percussion = 1,
    Stringed = 2,
    Wind = 3,
}

export enum HeaderNavigationIconId {
    Basket = 1,
    Login = 2,
}

export enum HeaderNavigationButtonLabel {
    Stringed = "Струнні",
    Wind = "Духові",
    Percussion = "Ударні",
}

export enum HeaderNavigationButtonPath {
    Stringed = "/stringed",
    Wind = "/wind",
    Percussion = "/percussion",
}

export enum HeaderNavigationIconAlt {
    Basket = "Basket",
    Login = "Login",
}

export enum HeaderNavigationIconTitle {
    Basket = "Basket",
    Login = "Login",
}

export enum HeaderNavigationIconPath {
    Basket = "/basket",
    Login = "/login",
}
